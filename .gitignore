node_modules/
data/db.json*
*.tmp

# secrets — never commit real credentials
.env

# process manager logs
logs/
*.log

# raw source workbook holds the same financial data we already keep out of git
*.xlsx

# NOTE: seed-data.json, clients.txt, emp details.txt and emp categories.txt hold
# real client/employee/financial data and are committed to this repo on purpose
# (see chat 2026-08-25). This repo is public, so that data is publicly visible.
